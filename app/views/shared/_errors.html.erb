<% if resource.errors.present? %>
  <div class="alert alert-danger">
    <h3><%= pluralize(resource.errors.count, 'error') %> detected:</h3>

    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>

  </div>
<% end %>
